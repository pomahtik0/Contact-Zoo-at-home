@model WebUI.Models.Pet.PetSelectionPage
@{
    ViewData["Title"] = $"Pets page {Model.Page}";
}

@foreach(var pet in Model.PetsOnPage)
{
    <partial name="_PetCard" model="pet" />
}

@section Scripts{
    <script src="~/js/PetBasketControll.js"></script>
}